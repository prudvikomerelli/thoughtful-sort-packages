# Thoughtful – Package Sorting

## Problem
Sort packages into STANDARD, SPECIAL, or REJECTED based on volume, dimensions, and mass.

## Run Tests
```bash
python -m unittest -v

git clone https://github.com/prudvikomerelli/thoughtful-sort-packages.git
cd thoughtful-sort-packages
python -m unittest -v
